const gameBoard = (() => {
    'use strict';

    const boardArray = ['x','0','x','0','x','0','x','0','x'];
    console.table(boardArray);
})();

const displayController = (() => {
    'use strict';
    console.log('display controls go here!')

})();